<div>
  <h1 class="font-bold base-text text-4xl">New Candle</h1>
  <div>
        <%= render(NavigationComponent.new) do %>
    <% end %>
    </div>

  <%= render "form", candle: @candle %>

  <%= link_to "Back to candles", candles_path, class: "button" %>
</div>
